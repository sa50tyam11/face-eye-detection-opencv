# 👁️ Real-Time Face & Eye Detection using C++ and OpenCV

A simple yet powerful face & eye detection system built using **C++** and **OpenCV**.  
The program uses Haar Cascade Classifiers to detect faces and eyes in real-time via webcam.

---

## 🚀 Features
- Detects faces and eyes live using your camera
- Real-time processing with bounding boxes
- Uses OpenCV’s Haar Cascade XML classifiers

---

## 🧠 Tech Stack
- **Language:** C++
- **Library:** OpenCV
- **Detection Models:** Haarcascade classifiers

---

## ⚙️ Setup Instructions

1. Install OpenCV  
   ```bash
   brew install opencv    # for macOS
   sudo apt install libopencv-dev   # for Linux

🚧 Future Improvements
Add smile detection
Integrate deep learning models (DNN Face Detector)
Build a GUI with Qt or OpenCV UI
